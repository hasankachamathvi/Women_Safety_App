<?xml version="1.0" encoding="utf-8"?>
<!-- activity_sos.xml - SOS Emergency Page Layout -->
<!-- Full pink/red screen with: -->
<!-- 1. Status text (shows instructions or "SOS Activated") -->
<!-- 2. Countdown number (3, 2, 1 when holding button) -->
<!-- 3. Big red SOS button (hold for 3 seconds to activate) -->
<!-- 4. Cancel button to go back -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#E91E63"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:id="@+id/tv_sos_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Emergency SOS"
        android:textColor="#FFFFFF"
        android:textSize="28sp"
        android:textStyle="bold"
        android:gravity="center" />

    <TextView
        android:id="@+id/tv_countdown"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text=""
        android:textColor="#FFFFFF"
        android:textSize="48sp"
        android:textStyle="bold" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_sos_main"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginTop="32dp"
        android:backgroundTint="#FF0000"
        android:text="SOS"
        android:textColor="#FFFFFF"
        android:textSize="48sp"
        android:textStyle="bold"
        app:cornerRadius="100dp"
        app:elevation="8dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_cancel_sos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:backgroundTint="#FFFFFF"
        android:text="Cancel"
        android:textColor="#E91E63"
        android:textSize="16sp"
        app:cornerRadius="24dp" />

</LinearLayout>
