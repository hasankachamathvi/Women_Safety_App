<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.yuwathi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission
        android:name="android.permission.SEND_SMS"
        tools:ignore="ProtectedPermissions,MissingPermission" />
    <uses-permission
        android:name="android.permission.CALL_PHONE"
        tools:ignore="MissingPermission" />
    <!-- Required companion feature tags for SEND_SMS and CALL_PHONE -->
    <uses-feature android:name="android.hardware.telephony"          android:required="false" />
    <uses-feature android:name="android.hardware.telephony.messaging" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Women_Safety_App"
        tools:targetApi="31">

        <activity android:name=".activities.SplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginActivity"    android:exported="true" />
        <activity android:name=".activities.RegisterActivity" android:exported="true" />
        <activity android:name=".activities.HomeActivity"     android:exported="true" />
        <activity android:name=".activities.LocationActivity"   android:exported="false" />
        <activity android:name=".activities.ContactsActivity"   android:exported="false" />
        <activity android:name=".activities.SafetyTipsActivity" android:exported="false" />
        <activity android:name=".activities.ComplaintActivity"  android:exported="false" />
        <activity android:name=".activities.Complaint2Activity" android:exported="false" />
        <activity android:name=".activities.SosActivity"
            android:exported="false"
            android:theme="@style/Theme.Women_Safety_App.SOS" />
        <activity android:name=".activities.ProfileActivity" android:exported="false" />

    </application>
</manifest>
