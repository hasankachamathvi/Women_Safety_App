rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules for this app so login/register + user flows + admin flows work.
    // Tighten these before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

